spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: raincar
    url: jdbc:mysql://localhost:3306/hospital?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 500
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

jwt:
  secret: jwtsecret

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace

